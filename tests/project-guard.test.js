const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.join(__dirname,'..');
test('canonical context files exist and are non-empty',()=>{for(const f of ['AI_START_HERE.md','PROJECT_RULES.md','MASTER_SPEC.md','PROJECT_STATUS.md','TEST_MATRIX.md']){const p=path.join(root,f);assert.equal(fs.existsSync(p),true,`${f} missing`);assert.ok(fs.readFileSync(p,'utf8').trim(),`${f} empty`)}});
test('canonical spec freezes list-first scan workflow',()=>{const spec=fs.readFileSync(path.join(root,'MASTER_SPEC.md'),'utf8');assert.match(spec,/通常スキャン完了を理由に詳細画面を自動で開かない/);assert.match(spec,/詳細はユーザーが対象商品をタップしたときに開く/)});
test('normal lookup must not request automatic detail opening',()=>{const app=fs.readFileSync(path.join(root,'app.js'),'utf8');const lookup=app.match(/async function lookup\(jan\)\{[\s\S]*?\}\nfunction reconcileSheet/);assert.ok(lookup,'lookup function not found');assert.doesNotMatch(lookup[0],/fetchKeepa\(jan,true(?:,|\))/,'normal lookup must not auto-open detail after Keepa')});
test('project rules prohibit silent context guessing and completion by report only',()=>{const rules=fs.readFileSync(path.join(root,'PROJECT_RULES.md'),'utf8');assert.match(rules,/推測して実装/);assert.match(rules,/Workの完了報告だけで合格判定/)});