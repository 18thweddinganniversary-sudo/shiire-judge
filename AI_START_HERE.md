# AI START HERE — 仕入れ判断

このファイルは、ChatGPT / Work / その他AIがこのリポジトリで開発作業を始める際の必須入口です。

## STOP GATE
コード・設定・テストを変更する前に、必ず次を読むこと。
1. `AI_START_HERE.md`
2. `PROJECT_RULES.md`
3. `MASTER_SPEC.md`
4. `PROJECT_STATUS.md`
5. `TEST_MATRIX.md`
6. 今回触る機能に対応する既存テスト

上記を取得・確認できない場合は **実装禁止**。推測で補完しない。

## Source of truth
確定仕様の正本は GitHub 上の `MASTER_SPEC.md` と、それを固定する自動テストです。チャット履歴・Memory・過去のWork報告は補助情報であり、正本ではありません。

## Mandatory development order
`正本確認 → 仕様差分確認 → 先に/同時に回帰テスト整備 → 最小実装 → 全テスト → Preview確認 → 本番反映 → 本番確認 → PROJECT_STATUS更新`

重要仕様を変更する場合、`MASTER_SPEC.md` と対応テストを同じ変更単位で更新すること。仕様とテストが矛盾する場合は実装を停止し、矛盾を解消してから進む。

## Non-negotiable
- 既存の確定仕様を、別不具合の修正ついでに変更しない。
- 新機能・基準緩和・大規模変更を勝手に追加しない。
- テストPASSだけを完成根拠にしない。本番挙動まで確認する。
- 実データ/APIを使うテストは、外部コスト・トークン消費を明示的に管理する。
- 不明・未実装は不明/未実装として扱い、推測で🟢を成立させない。

## Context-loss rule
過去会話を読めない、要約しかない、新しいセッションである、という理由で仕様を推測してはならない。このファイル群から現在地を復元する。復元できなければ作業を止める。
