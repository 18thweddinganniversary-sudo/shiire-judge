# PROJECT RULES

## 目的
仕入れ判断アプリの長期開発で、会話コンテキスト消失・AIの記憶漏れ・修正の副作用による堂々巡りを防ぐ。

## 最上位の共通開発原則
- 開発物の目的は、ユーザーを置き換えることではなく、ユーザーを支援すること。
- サポートを優先し、判断材料を分かりやすく整理して提示する。
- 最終判断・最終決定はユーザーに委ねる。アプリ側が命令・強制・断定で置き換えない。
- 人をアプリに合わせず、アプリを実際の生活・仕事・現場の流れに合わせる。
- 現実には途中変更・例外・気分・現場事情がある前提で設計する。
- 入力を増やす前に、履歴・実績・過去データを再利用してユーザーの手間を減らせないか検討する。
- 情報量を増やすことより、その場で必要な判断材料をすぐ出すことを優先する。
- 計画だけで終わらず、実際の行動・結果を記録し、次の支援に活かす。
- 成功・失敗・処理中を曖昧にせず、現在状態を明確にする。
- 机上の想定では完成扱いせず、実機・実生活・実店舗・実データで確認する。
- 新機能の数より、実際に使う機能の精度・信頼性・分かりやすさを優先する。

この原則は本アプリだけのローカルルールではなく、今後の開発物にも共通適用する設計思想として扱う。

## チャッピーループ防止原則
AIとの長期開発で、同じ調査・同じ修正・同じ確認依頼を繰り返す「チャッピーループ」を起こさない。

- 作業開始前に `MASTER_SPEC.md`、`PROJECT_STATUS.md`、`TEST_MATRIX.md` と現在のコードを正本として確認する。過去チャットの記憶だけで再開しない。
- 一度合格し、証拠が記録された項目は、回帰を示す新しい事実がない限り再調査しない。
- 新しい修正指示を出す前に、直前の指示内容 → 実際の実装差分 → 実機/本番結果を比較する。
- 問題の原因を「指示設計の不備」「実装の不備」「外部データ/API・環境要因」に分け、原因を決めずに次の修正を重ねない。
- ユーザーは実機事実を伝える役割を中心とし、仕様整理・技術判断・差分監査・合否判定をAI側からユーザーへ丸投げしない。
- 1項目を `調査 → 原因特定 → 修正 → テスト → 本番反映 → 本番確認 → 記録` まで閉じてから次へ進む。
- 同じ症状への修正が繰り返される場合は、場当たり修正を止め、直前までの指示・差分・ログを再監査して根本原因を特定する。
- スクリーンショットや手作業確認は、コード・ログ・API・自動テストで確認できない実機事実に限定する。ユーザーへ同じ確認を何度も要求しない。
- ユーザー操作が不要なところでは確認待ちで止まらず、利用可能なツールで進められる範囲を先に完了する。
- 「完了報告」を根拠に合格させない。コード差分、自動テスト、本番挙動、必要な実機結果を照合する。
- 合格した事実、未完事項、次の1項目を `PROJECT_STATUS.md` に残し、次回はそこから再開する。
- APIトークン、Workクレジット、ユーザーの時間など有限資源を、既に確認済みの再検証で浪費しない。

## 開発原則
- 賛同より検証を優先する。
- 根拠が弱い場合は未検証と明示する。
- ユーザー実機結果は重要な一次観測として扱い、コード/API/ログと照合する。
- 調査 → 修正 → テスト → 本番反映 → 本番確認を1項目ずつ完了する。
- 既に合格した項目を理由なく再調査しない。
- 技術判断をユーザーへ丸投げしない。

## 変更管理
- `main` を正本とする。
- 確定仕様は `MASTER_SPEC.md` に記録する。
- 現在地は `PROJECT_STATUS.md` に記録する。
- 重要な確定仕様は自動テストで固定する。
- 仕様変更時は、仕様・テスト・実装の整合を確認する。
- 変更完了時に `PROJECT_STATUS.md` を更新し、次回は過去チャットではなくそこから再開する。

## リリース合格条件
1. 対象仕様の回帰テストPASS
2. 全自動テストPASS
3. Preview/対象環境確認PASS
4. 本番反映PASS
5. 本番確認PASS
6. `PROJECT_STATUS.md` 更新

どれか未完なら完成扱いしない。

## 禁止
- テストを消す/弱めて実装を通す。
- 🟢を作る目的で仕入れ基準を勝手に緩める。
- 外部API失敗・データ不足を正常データとして補完する。
- 過去仕様が不明なときにAIが推測して実装する。
- Workの完了報告だけで合格判定する。
